import React from 'react'
import { ActivityIndicator } from 'react-native'

const Loader = () => {
    return (
        <ActivityIndicator size="large" className="w-full h-full" color="#0000ff" />
    )
}

export default Loader
